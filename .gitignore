# ── macOS ──
.DS_Store
Thumbs.db

# ── 构建产物 ──
ComateNotch/build/
ComateNotch/dist/
ComateHUD/ComateHUD-*.dmg
# 平台封面截图：每次发布重截都会换一份，不入库（发布时由 comate.sh code thumbnail 单独上传）
ComateHUD/thumbnail_*.png
ComateHUD/.publish/
dist/
build/
.output/
.next/
.nuxt/
.vite/

# ── 依赖 ──
node_modules/
.pnpm-store/

# ── 环境变量 ──
.env
.env.local
.env.*.local

# ── 编辑器 ──
.idea/
.vscode/

# ── 日志 ──
*.log
npm-debug.log*

# ── 临时文件 ──
tmp/
temp/
.cache/
.tmp/
.tmp-preview/
svgprev/
__pycache__/

# ── Shell ──
.bg-shell/

# ── Comate 平台元数据 ──
.wpscomate/
.inspect/
.cloudsync_meta.json
.cloudsync_zip_meta.json

# ── 内部工程笔记（仅本地，不进公开仓库）──
/AGENTS.md
/ARCHITECTURE.md
